import React from "react";
import ReactDOM from "react-dom/client";
import LoglandiaShell from "./LoglandiaShell.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LoglandiaShell />
  </React.StrictMode>
);
